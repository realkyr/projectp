import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  start: false,
  isLoadFinish: false,
  test: 'test'
}

export default new Vuex.Store({
  state,
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
